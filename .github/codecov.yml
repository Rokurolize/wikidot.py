coverage:
  status:
    patch: false
    project:
      default:
        informational: true

comment:
  layout: "reach, diff, flags, files"
  show_carryforward_flags: true

ignore:
  - "tests/**/*"

flags:
  unit:
    carryforward: true
    paths:
      - src/wikidot/
